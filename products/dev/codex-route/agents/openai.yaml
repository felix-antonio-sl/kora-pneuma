interface:
  display_name: "Codex Route"
  short_description: "Enruta superficie y pares Sol/Luna ratificados."
  default_prompt: "Usa $codex-route en modo route-only para proponer la superficie, uno de Sol high, Sol max o Luna max, el goal y el grafo mínimos."

policy:
  allow_implicit_invocation: false
