interface:
  display_name: "Codex Route"
  short_description: "Diseña explícitamente una ruta Sol/Luna y el grafo mínimo de sesiones."
  default_prompt: "Usa $codex-route para evaluar esta tarea en modo route-only y emitir la ruta mínima."

policy:
  allow_implicit_invocation: false
