# Handoff de KORA pneuma — 2026-08-09

Estado: mantenimiento documental verificado; la paridad runtime conserva drift
preexistente fuera del alcance de esta intervención.

## Corte observado

- Rama: `master`, alineada con `origin/master` en
  `1cd2a3636a8f87dc83fd5210e86cd076fd3fc416` al iniciar la auditoría.
- El árbol ya contenía trabajo concurrente sin cerrar en `kora.py`,
  `ley/3-transmutacion.md`, `tests/test_kora.py` e `IDEA.md`. Esta intervención
  no modifica ni adjudica esos cambios.
- Censo vivo: 497 artefactos — 434 de conocimiento, 27 agentes y 36 skills.
- Lifecycle: 349 `publicado`, 60 `activo`, 85 `borrador`, 2 `deprecado` y
  1 `retirado`.
- `velar --estricto`: 13 checks coherentes después de la intervención.
- Suite: 354 tests aprobados en 8,976 segundos.
- Navegación Markdown: 557 archivos revisados, 0 URN o rutas rotas detectadas.
- Paridad global: 121 `fiel`, 4 `desviada`, 24 `no-instalada` y
  0 `sin-emision`.

Los conteos y resultados anteriores pertenecen a este corte fechado; deben
repetirse si cambia el árbol.

Las cuatro desviaciones runtime —`cat-thinking` y `steipete` en Claude Code y
OpenCode— bloquean una promoción de esas unidades, pero no provienen de esta
intervención documental. `no-instalada` es informativo.

## Estructura documental vigente

- Introducción y navegación: `README.md`.
- Finalidad: `ALMA.md`.
- Especificaciones y decisiones normativas: `ley/0..4`.
- Fuentes gobernadas: `artefactos/`.
- Guía operativa detallada: `urn:kora:kb:guia-rapida-pneuma`.
- Contrato de trabajo para agentes: `AGENTS.md`; `CLAUDE.md` solo lo importa.
- Estado y dirección: este handoff, único documento activo de su especie.
- Historia fundacional: `GENESIS.md`, inmutable.

`censo.json`, `_emision/`, instalaciones runtime, `_archivo/`, `.remember/` y
`.superpowers/` no son fuentes de autoridad documental.

## Mantenimiento aplicado

- `README.md` pasó de lista de comandos a mapa de propósito, autoridad,
  vigencia, organización y entrada rápida.
- `AGENTS.md` adoptó español de Chile, fechas ISO y una política documental
  fechada con una sola versión activa por especie.
- El handoff raíz de 2026-08-03 se movió sin editar a
  `_archivo/handoff-2026-08-03-2.md`.
- La guía rápida dejó de duplicar inventarios temporales de corpus y targets;
  ahora remite al censo vivo y a `ley/3`.
- Se repararon 38 referencias no portables o mal interpretadas en fuentes
  canónicas: rutas locales se sustituyeron por URN resolubles, una fuente sin
  URN quedó como procedencia textual, una URL institucional obtuvo esquema
  HTTPS y una fórmula quedó protegida como código.
- Cada fuente gobernada modificada incrementó su versión y declaró la
  corrección editorial en `fuente:`.

## Vigencia y obsolescencia

Los únicos artefactos formalmente obsoletos para uso nuevo son:

- `urn:dev:artefacto:polymath` — `retirado`.
- `urn:salud:artefacto:analista-redes` — `deprecado`.
- `urn:salud:artefacto:constructor-tableros` — `deprecado`.

Sus URN siguen resolviendo por obligación de lifecycle. Los 85 borradores no
son obsoletos por definición: son material no publicado y requieren decisión
curatorial antes de promoción o descarte.

`censo --huerfanos` informó 161 candidatos sin arista entrante. El reporte es
informativo: incluye raíces de consumo directo y shards, por lo que no autoriza
deprecación, archivo ni eliminación automática.

## Próxima dirección

1. Cerrar por separado el candidato concurrente de Hermes. Mientras no exista
   un candidato Git integrado y verificado, `urn:kora:kb:regimen-de-ley`
   conserva la decisión publicada de Hermes como competencia legacy.
2. Si Hermes se acepta, sincronizar `regimen-de-ley` en el mismo candidato y
   probar forma, suite, emisión, paridad e instalación como capas distintas.
3. Curar los 161 candidatos del reporte de huérfanos por namespace y función,
   comenzando por conocimiento `publicado`; decidir `consumir`, `mantener como
   raíz`, `deprecar` o `reemplazar` con evidencia semántica.
4. Revisar los borradores GN heredados de KODA contra sus fuentes archivadas
   antes de publicarlos. Su forma válida no demuestra fidelidad editorial ni
   vigencia institucional.

## Límites

- `velar` prueba forma y coherencia mecanizada, no verdad semántica, vigencia
  externa, conducta runtime ni autorización humana.
- La auditoría reparó navegación y arquitectura documental; no reejecutó una
  koraficación contra las 434 fuentes originales.
- Las bitácoras locales ignoradas se clasificaron como scratch sin autoridad y
  no se reescribieron.
